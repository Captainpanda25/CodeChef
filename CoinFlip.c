#include<stdio.h>
int main()
{
int t,g,e,i,n,q
printf("Enter a test case:\n");
scanf("%d",&t);
printf("Enter number of games you want to play:\n");
scanf("%d",&g);
for(e=0;e<g;e++)
{
printf("Enter initial state of the coin, number of coins or rounds, total number of coins showing head/tail:\n");
scanf("%d %d %d",&i,&n,&q);
}
for(e=0;e<g;e++)
{
if(i==1)
{
for(f=0;f<n;f++)
a[f]=1;
for(f=0;f<n;f++)
{
a[f]=0;
if(
}

}
return 0;
}
